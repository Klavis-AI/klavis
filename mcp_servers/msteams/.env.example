# Microsoft Teams MCP Server Configuration

# Server Port (Optional)
# Port for the server to listen on in HTTP mode.
MS_TEAMS_MCP_SERVER_PORT=8791

# --- Azure Application Credentials ---
# These are required for the server to authenticate with the Microsoft Graph API.
# 1. Go to https://portal.azure.com/
# 2. Navigate to "App registrations" and create a new app.
# 3. Under "API permissions", add the required Application permissions (e.g., Team.Read.All, Channel.Read.All, etc.).
# 4. Make sure to grant admin consent for the added permissions.
# 5. Create a new client secret under "Certificates & secrets".

# Your Application (client) ID from the app's overview page.
MS_CLIENT_ID="your-client-id-here"

# The value of the client secret you created.
MS_CLIENT_SECRET="your-client-secret-here"

# Your Directory (tenant) ID from the app's overview page.
MS_TENANT_ID="your-tenant-id-here"