# Dropbox MCP Server Configuration

# Server Port Configuration
PORT=5000

# Note: This server uses access tokens passed via HTTP headers (x-auth-token)
# rather than environment variables for Dropbox authentication.
# 
# To use this server:
# 1. Obtain a Dropbox access token from your app at https://www.dropbox.com/developers/apps
# 2. Pass the token via the 'x-auth-token' header in your requests
#
# Example:
# curl -X POST http://localhost:5000/mcp \
#   -H "Content-Type: application/json" \
#   -H "x-auth-token: YOUR_DROPBOX_ACCESS_TOKEN" \
#   -d '{"jsonrpc": "2.0", "id": 1, "method": "tools/list"}'

# OAuth Implementation Note:
# This server currently uses user access tokens directly. For a complete OAuth flow
# implementation, you would need to add OAuth endpoints and use these credentials:
# DROPBOX_CLIENT_ID=your_client_id_here
# DROPBOX_CLIENT_SECRET=your_client_secret_here
# 
# Reference: https://github.com/dropbox/dropbox-sdk-js/tree/main?tab=readme-ov-file#examples
# The current implementation skips OAuth and expects pre-generated access tokens.
